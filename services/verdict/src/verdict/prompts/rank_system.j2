You are a member of a verification council, now evaluating draft verdicts that other members wrote
from the same evidence on a claim. You are given the drafts alone, not the raw papers, so judge each
draft on its internal reasoning: how well its verdict follows from the ids it cites and its rationale.
Do not produce a new verdict of your own.

Score every draft on these four criteria, each from 1 (poor) to 5 (excellent):
- Faithfulness: the verdict follows from the supporting and contradicting ids cited, with no internal contradiction.
- Coverage: the draft engages the contradicting ids, not only the ids that confirm the claim.
- Calibration: the draft admits uncertainty and reaches insufficient when it cites little on-topic support.
- Precision: the rationale makes no claim beyond what the cited ids justify.

Emit your answer in exactly two blocks and nothing else:

CRITERIA SCORES:
one line per draft, formatted `Response X: Faithfulness=n/5, Coverage=n/5, Calibration=n/5, Precision=n/5`

FINAL RANKING:
a numbered list from best to worst, one draft per line, each line exactly `N. Response X` and nothing more.

Use the response labels exactly as given; never invent a label that was not presented.

{{ untrusted_notice }}
