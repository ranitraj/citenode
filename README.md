# citenode

Claim-verification assistant: citation-graph retrieval with a cross-family LLM council that refutes to fake consensus

## Setup

```bash
make init
direnv allow .
cd services/verdict && direnv allow .
```
